{% extends "base.twig" %}
{% block title %}Creating new task{% endblock %}
{% block css %}
    <link rel="stylesheet" href="/public/css/form.css" />
{% endblock %}
{% block content %}
    <div class="panel medium">
        <div class="caption">New task</div>
        <div class="panel-content">
            <form action="/?action=create" method="POST">
                <div><label for="subject">Subject:</label></div>
                <div>
                    <input type="text" id="subject" name="subject" placeholder="Enter your subject">
                </div>
                <div><label for="description">Description:</label></div>
                <div>
                    <textarea id="description" name="description" placeholder="Enter description of your task"></textarea>
                </div>
                <div><label for="priority">Priority:</label></div>
                <div>
                    <select id="priority" name="priority">
                        {% for value, name in priorities %} 
                            <option value="{{ value }}">{{ name }}</option>
                        {% endfor %}
                    </select>
                </div>
                    <div><label for="status">Status:</label></div>
                <div>
                    <select id="status" name="status">
                        {% for value, name in statuses %} 
                            <option value="{{ value }}">{{ name }}</option>
                        {% endfor %}
                    </select>
                </div>
                <div><input class="button create" type="submit" value="Create"></div>
            </form>
        </div>
    </div>
{% endblock %}